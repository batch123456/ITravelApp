@model IEnumerable<TravelMVC.Models.Admin>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdminPassword)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RememberMe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdminPassword)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RememberMe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdminEmail }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdminEmail }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdminEmail })
        </td>
    </tr>
}

</table>
